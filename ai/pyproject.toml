[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "ostosense-contract"
version = "0.1.0"
description = "OSTOSENSE Tier 1 data contract and logger"
requires-python = ">=3.10"
dependencies = []

[tool.setuptools.packages.find]
where = ["src"]
